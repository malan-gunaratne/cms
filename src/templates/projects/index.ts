import { Block } from 'payload/types'

export const Page: Block = {    
    slug: 'projectsPage',
    fields: [
        {
            name: 'title',
            label: 'Title',
            type: 'text'
        }
    ]   
}
